import React from 'react';

const MovieList = ({ movies, title }) => {
    // console.log(movies);
    // console.log(title);


    return (
        <>
            <h1>MovieList</h1>
        </>
    )
}

export default MovieList;